<p>ショッピングカート</p>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>ジャンル</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
     <% @items.each do |item| %>
     <%# genre.genre_id.each do |genre| %>
     <tr>
       <td><%= item.name %></td>
       <td><%= item.price %></td>
       <td><%#= order.amount %></td>
       <td><%#　小計　 %></td>
       <td><%= link_to "削除する", cart_items_path, method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
     </tr>
       <% end %>
       <%# end %>
  </tbody>
</table>