
<h6>ショッピングカート</h6>

  <table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <td></td>
    </tr>
  </thead>
  <tbody>
      <% @cart_items.each do |item| %>
     <%# @cart_items.each do |item| %>
     <tr>
       <td><%= item.item.name %></td>
       <td><%#= item.price %></td>
       <td><%= item.amount %></td>
       <td><%= %></td>
     </tr>
       <% end %>
  </tbody>
</table>

