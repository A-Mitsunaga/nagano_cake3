<h1>会員一覧</h1>

<table>
  <thead>
    <tr>
      <th>会員ID</th>
      <th>氏名</th>
      <th>メールアドレス</th>
    </tr>
  </thead>
  <tbody>
     <% @customers.each do |customer| %>
     <%# genre.genre_id.each do |genre| %>
     <tr>
       <td><%= customer.id %></td>
       <td><%= link_to customer.last_name, "/admin/customers/#{customer.id}" %></td>
       <td><%= customer.email %></td>
     </tr>
       <% end %>
       <%# end %>
  </tbody>
</table>