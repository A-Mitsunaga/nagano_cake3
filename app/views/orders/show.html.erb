<h5>注文履歴詳細</h5>

<strong>注文情報</strong>
<p>注文日</p><%= @order.created_at %>
<p>配送先</p><%= @order.postal_code %><%= @order.address %><%= @order.name %>
<p>支払方法</p><%= @order.payment_method %>

<br>
<strong>注文内容</strong>
<br>

<table>
  <thead>
    <tr>
      <th>商品</th>
      <th>単価（税込）</th>
      <th>個数</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
     <tr>
<% @cart_items.each do |item| %>
       <td><%= item.item.name %></td>
       <td><%= item.item.price %></td>
       <td><%= item.amount %></td>
       <td><%= (item.item.price * item.amount) %></td>
       <% end %>
     </tr>
  </tbody>
</table>


<srrong>請求情報</srrong>

  <% sum = 0 %>
   <% @cart_items.each do |item| %>
   <!--billing_amount-->
    <% sum += (item.item.price * item.amount) %>
     <% end %>
<strong>送料</strong><%= @order.shipping_cost %>
<strong>商品合計</strong><%= sum %>
<strong>請求金額</strong><%= sum + @order.shipping_cost %>
<br>
<br>