<h3>商品一覧</h3>

<% @items.each do |item| %>
<%= attachment_image_tag item, :image, format: 'jpeg', size: "100x100" %>
<div class="container">
<!--<span><%#= link_to item.name, "/items/#{item.id}" %></span>-->
<!--<span>¥<%#= item.price %></span>-->
  <div class="row">
    <div class="col-lg-3">
      <span><%= link_to item.name, "/items/#{item.id}" %></span>
      <span>¥<%= item.price %></span>
    </div>
  </div>
  
  
  
  
  
</div>



<% end %>